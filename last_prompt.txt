<USER_REQUEST>
# SmartCompare Pro — Exact Fix Prompt for Multi-Store Comparison

## Root Cause Analysis (from codebase)

The bug has **three connected causes** traced through the actual code:

### Cause 1 — Cold path returns only 1 result immediately (compare.routes.js)

In `backend/src/routes/compare.routes.js`, the cold path (new product, no DB match) does this:

```js
// Cold path — no match found
const productId = await createNewProduct(...)
await linkListingToProduct(url, productId)
jobIds = await jobEnqueuer.enqueueAllPlatforms(productId, platform)
processScrapeJobs().catch(...)           // fires in background — NOT awaited
const listings = await listingFetcher.fetch(productId)  // fetches BEFORE scrapes finish
const assembled = await responseAssembler.assemble(productId, listings, req.userId)
return res.json({ status: "queued", partial: true, ...assembled })
```

`processScrapeJobs()` is non-awaited. `listingFetcher.fetch()` runs immediately after, so it only finds the 1 listing just observed by the extension (the current store). The other platform scrape jobs exist in the DB queue but haven't run yet. Result: panel shows only 1 store.

### Cause 2 — Happy path also returns 1 result when scrapes haven't finished (compare.routes.js)

In the happy path (product already in DB):

```js
const listings = await listingFetcher.fetch(productId)
// ...
if (results.length >= 2) { return found }
// if fewer than 2: enqueue and return partial
```

This correctly returns `partial` when only 1 result exists, but the extension then polls `runComparison(true)` — which re-hits `POST /api/compare` and goes through the exact same flow, fetching listings before scrapes finish. The poll hits the DB too early.

### Cause 3 — Extension polls compare endpoint instead of a results endpoint (content.js)

In `extension/content.js`, when `status === 'partial'` or `'queued'`, the extension calls `runComparison(true)` — this re-posts to `POST /api/compare`, which re-resolves the pro
<truncated 9925 bytes>
ly category-appropriate platforms
- The `renderCompareSection` function in `content.js` — already correctly renders multi-store results when `data.results` has more than 1 entry
- The `GET /api/compare/results/:job_id` endpoint — keep it but it is not used in this fix (it returns job status only, not listings)

---

## Verification Test

After applying the fix:

1. Open a fresh product page on Amazon (one never visited before).
2. Open extension panel — should show 1 result (Amazon) with a spinning poll indicator.
3. Wait 15–45 seconds — panel should progressively update showing Flipkart, Croma, Reliance Digital, Vijay Sales rows as scrapers complete.
4. Each row's `href` (the URL the scraper landed on after following the search URL) must open the actual product page, not a search results page — this already works because `amazon.scraper.js`, `flipkart.scraper.js`, etc. all navigate from search URL → first matching product page and store that final URL in `listings.url`.
5. Open same product page again (2nd visit) — should immediately show all stores (DB cache hit via `listingFetcher.fetch`).
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-05-21T17:26:33+05:30.

The user's current state is as follows:
Active Document: c:\Users\tanis\OneDrive\Desktop\VIT subjects\SEM-4\EDI\SmartComparison-Tool\extension\background.js (LANGUAGE_JAVASCRIPT)
Cursor is on line: 1
Other open documents:
- c:\Users\tanis\OneDrive\Desktop\VIT subjects\SEM-4\EDI\SmartComparison-Tool\extension\background.js (LANGUAGE_JAVASCRIPT)
Running terminal commands:
- docker-compose up --build (in c:\Users\tanis\OneDrive\Desktop\VIT subjects\SEM-4\EDI\SmartComparison-Tool, running for 58m25s)
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from None to Gemini 3.5 Flash (High). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>
<USER_REQUEST>
Now I have a complete picture of the entire system. Let me synthesize all findings into a precise, actionable prompt.Now I have a complete, deep understanding of the entire system. Let me produce the exact prompt.I now have an extremely deep and complete picture of the full system. Here is the exact, comprehensive prompt:

---

## Exact Prompt to Fix the Comparison Panel

> **Copy this entire prompt and give it to your developer (or use it with an AI coding assistant on this codebase):**

---

```
You are working on SmartCompare Pro — a browser extension + Node.js backend that compares 
prices across Indian e-commerce platforms. There is a critical bug: the comparison panel 
always shows "We couldn't find this product on other stores right now" and only ever 
displays the current website's listing, never other platforms.

Perform a deep fix across both the extension and the backend. Here is the full root-cause 
analysis and every change required:

═══════════════════════════════════════════════════════
ROOT CAUSE ANALYSIS
═══════════════════════════════════════════════════════

CAUSE 1 — SEARCH URLs ARE BEING STORED AS LISTING URLS (Backend: scraper.runner.js)
The scraper.runner.js `upsertListingAndPrice` function stores `data.url || job.listing_url` 
as the listing URL. When scrapers are given a search URL (e.g. 
`https://www.flipkart.com/search?q=samsung+galaxy`) and the scraper navigates to the actual 
product page and returns `data.url` = the real product URL, that is correct. BUT if a 
scraper fails to set `data.url` (returns undefined or null), it falls back to 
`job.listing_url` which IS the search URL. These search URLs then get stored in the 
`listings` table.

In `extension/content.js`, line ~887-888, these search URLs are filtered OUT o
<truncated 10844 bytes>
what each fix targets:

**The core problem** is a cascade of 4 issues:

1. **Scrapers start with a search URL** (e.g. `flipkart.com/search?q=iPhone`) → they navigate to the product page → but if they fail, the **search URL itself gets stored** in the DB as a "listing"

2. **The extension frontend then correctly filters out search URLs** from the comparison list — but this means *all failed scrapers get silently dropped*, leaving only the current store

3. **`externalCount === 0` immediately shows "not found"** even when the backend just hasn't finished scraping yet (status = "queued") — there's no grace period

4. **Polling may not be fetching assembled listing data** — it might only be fetching job status, so the UI never refreshes when scrapes complete

Fix 1+2 clean the backend data pipeline. Fix 3+4 fix the frontend display logic. Fix 5 makes the URL filter more precise. Fixes 6+7 ensure the UX always shows something useful and visit buttons go to real product pages.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-05-21T17:53:22+05:30.

The user's current state is as follows:
Active Document: c:\Users\tanis\OneDrive\Desktop\VIT subjects\SEM-4\EDI\SmartComparison-Tool\extension\background.js (LANGUAGE_JAVASCRIPT)
Cursor is on line: 1
Other open documents:
- c:\Users\tanis\OneDrive\Desktop\VIT subjects\SEM-4\EDI\SmartComparison-Tool\extension\background.js (LANGUAGE_JAVASCRIPT)
- c:\Users\tanis\OneDrive\Desktop\VIT subjects\SEM-4\EDI\SmartComparison-Tool\backend\tests\compare.route.test.js (LANGUAGE_JAVASCRIPT)
Running terminal commands:
- docker-compose up --build (in c:\Users\tanis\OneDrive\Desktop\VIT subjects\SEM-4\EDI\SmartComparison-Tool, running for 1h25m14s)
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from  to Gemini 3.1 Pro (High). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>
